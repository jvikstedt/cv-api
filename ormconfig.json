{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "cv_user",
  "password": "cv_password",
  "database": "cv_db",
  "synchronize": true,
  "logging": false,
  "entities": [
     "src/**/*.entity.ts"
  ],
  "migrations": [
     "database/migration/**/*.ts"
  ],
  "subscribers": [
     "src/**/*.subscriber.ts"
  ],
  "seeds": [
    "database/seeds/**/*.ts"
  ],
  "factories": [
    "src/**/*.factory.ts"
  ]
}
